// 占い結果の組み合わせ生成（一言アドバイス/ラッキーカラー/ラッキーアイテム）で使う乱数取得を薄くラップする。
// 現時点ではMath.randomをそのまま使うだけだが、将来「同じシードなら同じ結果を再現したい」という要件が
// 出た場合に、この関数の中身だけ差し替えれば済むようにしておく（呼び出し側のAPIは変更不要）。

/**
 * @returns {number} 0以上1未満の乱数
 */
export function defaultRng() {
  return Math.random();
}
